import type { AppUser } from '../App'
export default function Tablon({ user: _ }: { user: AppUser }) {
  return <div className="p-4"><h1 className="text-xl font-bold">Mercado de Fichajes</h1></div>
}
